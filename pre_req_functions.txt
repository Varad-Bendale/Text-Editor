In the raw mode we removed each and every function already so now we now completely define all the functions from core again specifically for the text editor 
First we defined the ctrl key see here we have some number and then we do the & with the 0x1f which just indicates that that the ctrl button is clicked or that
flags are edited that way like we got the ascii characters for  all the commands and then here we want it to be like 
Now here we are defining the ctrl key cause see we don't want to write the ascii key / number again and again so we just defined it like this so that if we 
just put it in the k & 0xf1 we directly get the ascii number simple. 
Lets begin with ending the function with ctrl + q 
Okay now we are making the new function for reading all the terms why cause  see in the very first it was not waiting for the alphabets to be written it was 
continuously  throwing the 0 now it waits for the user to enter the term first also we made these functions cause we have some new functions needed to be 
added and its just better easier and less of a mess to handle . 
Now listen this is where we kind of split ways with kilo 
Kilo render system is like you type something all the characters are given to the terminal and then terminal creates 1 huge thing or render and then give
it to the user where as in today current optimised text editors use the difference systems like you look in the coordinates of the stuff and then whatever
is changed only those needs to be changed the rest remains the same 

So for now we are skipping few functions and continuing with the functions which are needed regardless any of the algorithm is used 
Now listen there was this vt100 emulator a very old age emulator on basis of this these commands were designed on if you can see
\x1b is escape button 
[ this ensures that we are on the escape function now currently 
2 represents that how much of the screen needs to be clear like there are multiple options we can like have the till cursor or full or after cursor like 
those I am speaking of 
And J is usually used to clear out the screen 
These commands are known as escape commands these often start with escape word with ] followed with it and there are often used in like moving cursor or 
clearing the screen or adding some colour or something like that 
Now after clearing we see that the cursor is not at the top most left side its anywhere the thing left on the page so we fix it same we use the escape 
function 
See here the code is like \x1b[H again h represents the functions regarding the cursor and the as here we want it to point to the start so we leave it 
like this but if we want the cursor to somewhere else we do like this 
\x1b[8;4H these are like coordinates we have 
Okay we are going to keep these functions in the end of the programme as well cause if any of the error happens we don't need it to post any of the 
random garbage it just clears out simply 
Now see we could have done added the commdns in the ataxia itself but if any of the errors occur we dont want them to disappear so e keep it at this 
point so any error occurs we get it cuase atexit works regardless the the compilation issue occurs or not 

We add tlides in the txt editor like just for majority to like its there for understanding liek these many columns or that is there for user I mean
it just looks good nothing else 

See when we like need to clear out the screen like see right now the text editor will be on terminal so now we need to like clear the whole terminal 
like a2z everything first and then we edit on that space so now work for it now see for this one we have 
Okay number 1 we first define a global structure for storing like all the rows and the columns of the terminal for rendering we store all those values
in this one part so for now we are storing all the original strut which we used t keep the flags of the terminal of the settings of the terminal stored
in this one 
Okay many of the devices do not support this ioctl its fast and cool but the terminal based thing maybe some places may not be there so we need to add
a manual way as well for calculating window size the thing here is we just do the cursor move down by 999 and to the right by 999 now there is strict
guidelines to that the cursor cannot move greater than the size of the terminal so we can get the exact size of the window by calculating it 


