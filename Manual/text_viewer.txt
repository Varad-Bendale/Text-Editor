Okay here we deal up with the text which we have to display it and opening the already written text which are here in the file or something like that 
First we make a strict for the input which we have the 
Typedef is simpler version of the structure we don't have to write struct again and again it just does the job easily and it contains the data and the 
size of the data which is there 
We add the attribute of number of rows in the thing and update it as we do the thing is we just write whatever we want we then allocate the memory do
the malloc update the struct which we have created with the size as per the length is of the text and then we do the malloc to the data attribute of
the sturct see we do len + 1 cuase there is one \0 which we need to consider and add the malloc for it as well its usefulness is there as we can
identify that the sentence is ended here and stuff like that then we memcpy copy all the contents present in the line to the data attribute in the
struct and we then update the row count which we have in the main struct
Malloc syntax is like 
  
Now here we kind of open up the file and then open it for reading purpose and then we do the getline what it does is it takes all the content in the
file and then Put it in the lines or part we want to put it in and if we assign it to the variable we can get the length of the thing as well and now
here in the string may contain \n\r we kind of want to skip those as well as they are of no use so we do it and in the very end the memory which we
gave to the lines we free them up so that it don't take the excess stupid memory 

Okay in the main function we pass the arguments like first int argc means the number of arguments which are present and the other one is directly the 
argument character like lets suppose 
./txt varad.txt now here the ./txt is 1 argument and the varad.txt is other one so its like 2 different arguments and we want to send it to the function
so we just pass the argument 2 which is the file name pass it to the thing 

Okay for the multiple line we put the thin in the loop as the lines we are using is in the array * this thing signifies this thing and we just put in the
loop directly the auto increment occurs and then we start appending the lines in the buffer which we have created first of all we need to reallocate the memory of 
struct contents * number of lines 
Now we need to do it every single time cause our number of lines is increasing every single time 

Okay here comes the interesting part now see if the file is huge huge and we want to scroll thorugh the file we keep one attribute known as offset it
refers to the first line of the page anywhere like suppose the cursor is on something on line 520 and the page starts at the 500 so the offset is 500 
here so its like it and by default we keep it 0 so that whenever we start it points the start of the stuff and now we head towards the scrolling the things 

See we have the offset like somewhere the page is upwards and the cursor is below see this happens in like the searching stuff you searched something 
in the file you got the offsets of 500 but your cursor is just on the 50 so it comes to it so like a backup stuff 
And on the next part if you can see if the cursor is gone wayyy further the offset is below then suppose the offset is at 20 and if the cursor is 
nowhere inside the screen in the thing and is very very far away so we then keep the offset as the cursor which is there - (the first term is needed )
so screen rows 
This settles our cursor stuff only needed is the cursor showing or displaying we do it in the snprintf thing there we need to update if the cx is 
gone way way huge we need to reduce it with the offset so that we can get the correct path also we have restricted the arrow down part we remove 
restrictions over it as well 

Okay see now we have few modifications in the txt print stuff okay see first of all were use this one here 
		 int correct_row = i + edit.row_offset ; 
 
So here are just taking all the things in the account before doing anything so like only the parts which we need to see will only be started 
from here its like that and as we have the protocol to clear the screen line by line as it is faster and better so it removes all the line 
first and then we append all the buffer lines which we have allocated to the particularly row ( correct_row ) this one and then from there 
we just we append all the text which are present in the stuff till the screen end and then print it see this is inversely true reverse as 
well as the cursor position is handled inversely	as well the offset needs to follow the cursor only so then our offset gets corrected 
and then we can do it 

Now see here na the while loop works continuously anything different come we  do the work so now here we like change initialise the buffer 
every single time so that just the part which we want only come and we print it why not free it if we do then the st


----------------------------------------
Hey clear the doubt about the malloc and the realloc you had 

---------------------------------------


Okay we enable scrolling of the horizontal as well majority of the procedure is same as the row offset things change only at the printing 
stuff so like see first we were just passing out the data to the thing now what we do is we were passing entire thing in here 
edit.ri[correct_row].data 
This gave the address of the entire string to us and then the thing was printed but now we need to like as we go or scroll the thing we 
need to start the thing to printing from there onwards so now 
&edit.ri[correct_row].data[edit.col_offset] 
& is needed as it is the pointer towards the specific word here 
Now also we need to print this things so the affect the part of the length of bytes of the text is so we need to like add the edge cases
which causes the problem like these see the difference of the cursor and the offset can be negative so we need to make it correct as the
byte size cannot be negative noe 0 also and done set 
