Okay here we deal up with the text which we have to display it and opening the already written text which are here in the file or something like that 
First we make a strict for the input which we have the 
Typedef is simpler version of the structure we don't have to write struct again and again it just does the job easily and it contains the data and the size of the data which is there 
We add the attribute of number of rows in the thing and update it as we do the thing is we just write whatever we want we then allocate the memory do the malloc update the struct which we have created with the size as per the length is of the text and then we do the malloc to the data attribute of the sturct see we do len + 1 cuase there is one \0 which we need to consider and add the malloc for it as well its usefulness is there as we can identify that the sentence is ended here and stuff like that then we memcpy copy all the contents present in the line to the data attribute in the struct and we then update the row count which we have in the main struct
Malloc syntax is like 
  
Now here we kind of open up the file and then open it for reading purpose and then we do the getline what it does is it takes all the content in the file and then Put it in the lines or part we want to put it in and if we assign it to the variable we can get the length of the thing as well and now here in the string may contain \n\r we kind of want to skip those as well as they are of no use so we do it and in the very end the memory which we gave to the lines we free them up so that it don't take the excess stupid memory 

Okay in the main function we pass the arguments like first int argc means the number of arguments which are present and the other one is directly the argument character like lets suppose 
./txt varad.txt now here the ./txt is 1 argument and the varad.txt is other one so its like 2 different arguments and we want to send it to the function so we just pass the argument 2 which is the file name pass it to the thing 

Okay for the multiple line we put the thin in the loop as the lines we are using is in the array * this thing signifies this thing and we just put in the loop directly the auto increment occurs and then we start appending the lines in the buffer which we have created first of all we need to reallocate the memory of 
struct contents * number of lines 
Now we need to do it every single time cause our number of lines is increasing every single time 

Okay here comes the interesting part now see if the file is huge huge and we want to scroll thorugh the file we keep one attribute known as offset it refers to the first line of the page anywhere like suppose the cursor is on something on line 520 and the page starts at the 500 so the offset is 500 here so its like it and by default we keep it 0 so that whenever we start it points the start of the stuff and now we head towards the scrolling the things 

See we have the offset like somewhere the page is upwards and the cursor is below see this happens in like the searching stuff you searched something in the file you got the offsets of 500 but your cursor is just on the 50 so it comes to it so like a backup stuff 
And on the next part if you can see if the cursor is gone wayyy further the offset is below then suppose the offset is at 20 and if the cursor is nowhere inside the screen in the thing and is very very far away so we then keep the offset as the cursor which is there - (the first term is needed ) so screen rows 
This settles our cursor stuff only needed is the cursor showing or displaying we do it in the snprintf thing there we need to update if the cx is gone way way huge we need to reduce it with the offset so that we can get the correct path also we have restricted the arrow down part we remove restrictions over it as well 

